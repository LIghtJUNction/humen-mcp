{
  "jsonrpc": "2.0",
  "id": 6,
  "method": "tools/call",
  "params": {
    "name": "ask_humen_judgment_async",
    "arguments": {
      "title": "Approve rollout",
      "prompt": "Can the service rollout continue after reviewing the smoke test result?",
      "steps": [
        "Check that healthz returned ok.",
        "Check that tools/list includes ask_humen.",
        "Answer yes only if both checks passed."
      ],
      "timeout_seconds": 300
    }
  }
}
